# 3D 中国象棋 Web 原型开发方案

## 1. 项目定义

### 1.1 一期目标

一期交付一个可在现代浏览器中直接游玩的 3D 中国象棋原型，优先验证三个核心问题：

1. 标准中国象棋规则是否完整、稳定，能够完成一局本地双人对战。
2. 3D 棋盘和“古代铠甲”棋子是否兼顾中国风氛围与棋局辨识度。
3. 移动、落子和吃子特效是否有战斗感，同时不拖慢连续操作。

目标平台为桌面浏览器，并保证手机浏览器可以完成整局操作。一期使用程序化几何体、基础材质和轻量粒子完成视觉原型；所有棋子底座保留传统汉字，造型创新不得牺牲识别效率。

### 1.2 产品原则

- **规则优先**：规则状态是唯一事实来源，3D 动画只表现已经确认的合法走法。
- **局面清楚**：任意常用视角下，阵营、棋子类型、选中状态和合法落点都可辨认。
- **特效短促**：普通移动不超过 0.6 秒，吃子主动画不超过 1.2 秒，并允许关闭镜头震动和降低特效。
- **渐进增强**：WebGL 或高画质能力不足时降低阴影、粒子和后期效果，不影响下棋。
- **模块可替换**：一期程序化棋子、规则实现、特效和音频均通过稳定接口接入，二期可单独替换为正式资产或外部引擎。

### 1.3 一期非目标

- 联网匹配、房间、观战、账号和排行榜。
- 象棋 AI、人机难度和残局分析。
- 高精度人物模型、逐棋子骨骼动画、动作捕捉和完整战场场景。
- 皮肤、商城、付费、赛季等商业化系统。
- 棋谱导入导出、云存档和跨设备同步。
- 中国象棋竞赛规则中复杂的长将、长捉和循环局面裁决；一期不自动进行争议性判罚。
- 旧版浏览器、无 WebGL 环境及原生桌面/移动客户端适配。

## 2. 技术方案

### 2.1 技术栈

| 范畴 | 方案 | 选择理由 |
| --- | --- | --- |
| 工程 | Vite + TypeScript | 启动快、构建简单、类型约束明确 |
| UI | React | 适合状态面板、菜单和响应式交互 |
| 3D | Three.js + React Three Fiber + Drei | 场景能力成熟，便于组件化棋盘、棋子和镜头 |
| 状态 | React reducer 或 Zustand | 将棋局状态与场景渲染解耦，避免动画直接修改规则数据 |
| 动画 | React Three Fiber 帧循环 + 补间工具 | 适合短时位移、弹跳、材质闪烁和粒子生命周期 |
| 规则 | 纯 TypeScript `RuleEngine` 接口；优先适配经依赖审计的成熟象棋规则库 | 避免 UI 绑定某个库，并通过固定棋局测试补足边界行为 |
| 单元测试 | Vitest | 与 Vite/TypeScript 集成成本低 |
| 端到端测试 | Playwright | 验证浏览器操作、响应式布局、WebGL 加载和主要对局流程 |
| 质量 | ESLint + Prettier + `tsc --noEmit` | 统一代码风格并阻止明显类型回归 |

规则库必须锁定精确版本并由 `RuleEngine` 适配层隔离。依赖审计至少确认：支持将帅照面、马腿、象眼、炮架、将军和绝杀判断；若候选库任一项不可靠，则在不改变上层接口的前提下使用纯 TypeScript 规则实现，并以本方案的规则测试作为交付门槛。

### 2.2 分层架构

```text
浏览器输入
   │
   ▼
交互层 InputController / HUD
   │  发出“选择、尝试走子、设置变更”意图
   ▼
应用层 GameController / GameState
   │  调用规则、提交不可变棋局状态、驱动回合状态机
   ├──────────────► 规则层 RuleEngine
   │                合法着法、将军、胜负、棋谱
   ▼
表现层 Scene / Board / Piece / Effects / Camera
   │  消费状态与表现事件，不决定走法是否合法
   ▼
Three.js / WebGL
```

推荐代码边界：

```text
src/
├─ app/                 # 应用入口、全局布局、路由（如需要）
├─ game/
│  ├─ model/            # Piece、Position、Move、GameStatus 类型
│  ├─ rules/            # RuleEngine 接口、规则适配器、局面序列化
│  ├─ state/            # reducer/store、回合状态机、走子历史
│  └─ use-cases/        # 选子、走子、重开、悔棋
├─ scene/
│  ├─ board/            # 棋盘几何、网格、楚河汉界、落点标记
│  ├─ pieces/           # 棋子工厂、阵营主题、字符纹理
│  ├─ effects/          # 移动轨迹、落地波纹、吃子爆发、对象池
│  ├─ camera/           # 斜俯视、顶视、缩放与复位
│  └─ lighting/         # 灯光、阴影、性能档位
├─ ui/                  # 回合、将军/胜负、操作按钮、设置面板
├─ audio/               # 音效接口；一期允许只接基础占位音效
└─ tests/               # 固定局面、集成和端到端测试数据
```

### 2.3 核心模块契约

#### 规则层 `RuleEngine`

只接受棋盘坐标和棋局数据，不依赖 React、Three.js 或动画时间。

```ts
interface RuleEngine {
  getState(): GameState;
  getLegalMoves(from: Position): Move[];
  tryMove(move: Move): MoveResult;
  undo(): GameState | null;
  reset(): GameState;
  getStatus(): GameStatus;
}
```

`MoveResult` 至少包含移动方、起点、终点、被吃棋子、将军状态、胜负状态和新的局面编号。表现层依赖这些字段生成动画，不重新推导规则。

#### 回合状态机

```text
READY → PIECE_SELECTED → ANIMATING → READY
  │            │              │
  └────────────┴──────────────┴──► GAME_OVER
```

- `READY`：允许选择当前行棋方棋子。
- `PIECE_SELECTED`：显示合法落点；可改选己方棋子或取消。
- `ANIMATING`：规则状态已经提交，锁定棋盘输入；允许设置和跳过表现动画。
- `GAME_OVER`：只允许重开、悔棋或查看棋盘。

#### 坐标系统

- 规则坐标固定为 `file: 0..8`、`rank: 0..9`，红方初始位于高 rank。
- 3D 世界坐标统一由 `boardToWorld(position)` 转换，禁止棋子组件自行计算格距或翻转。
- 切换己方视角只旋转相机或场景观察方向，不改变规则坐标。

#### 表现事件

应用层只发布有限事件：`piece-selected`、`move-started`、`piece-captured`、`check`、`game-over`、`undo`。移动与吃子共用一条可取消时间线，悔棋或重开时必须清理旧粒子、补间和延迟回调。

### 2.4 一期视觉实现

- 棋盘：深色木质基座、金属包边、凹刻棋盘线、清晰的楚河汉界与九宫线。
- 红方：朱红漆甲、铜金护片、暖色轮廓光；整体轮廓偏轻捷、向上。
- 黑方：黑铁重甲、银灰护片、冷色轮廓光；整体轮廓偏厚重、外扩。
- 棋子：使用“统一底座 + 阵营甲胄主体 + 类型标志件”构成。马、车、炮优先做明显轮廓，其余棋子至少通过高度、冠饰、武器和底座汉字区分。
- 选择反馈：底座阵营色光环、轻微抬升、合法交点阵纹。
- 普通移动：短促抬升/突进、残影或尘粒、落点波纹。
- 吃子：攻击方突进、接触闪光与冲击环、被吃方碎甲/光粒消散；炮增加爆燃，马增加跃动弧线。
- 镜头：默认斜俯视、顶视、复位；击杀时只做轻微推进和震动，不强制切换到遮挡棋局的电影镜头。

## 3. 一期功能范围

### 3.1 必须完成

- 标准初始棋局和红方先行。
- 车、马、炮、兵/卒、相/象、仕/士、帅/将的合法走法。
- 马腿、象眼、象不过河、仕/将九宫限制、兵卒过河、炮架和将帅照面。
- 禁止走后己方仍被将军的着法。
- 将军、无合法着法、绝杀/困毙与胜负结果提示。
- 点击选子、合法落点提示、点击落子、取消选择。
- 本地红黑双方轮流操作。
- 悔棋、重开、镜头复位、顶视/斜视切换。
- 3D 棋盘、双方差异化程序化棋子和底座汉字。
- 通用移动、落地、吃子效果，以及马和炮的类型差异化表现。
- 当前回合、将军、胜负、最近一步着法的界面反馈。
- 特效强度与镜头震动开关；设置保存在浏览器本地。
- 桌面和移动端基础响应式布局。

### 3.2 可延后项

- 背景音乐和完整音效包；一期可使用少量合法授权的占位音效或静音交付。
- 完整招法记谱；一期至少保存结构化 `Move[]`，界面可只显示最近一步。
- 棋盘翻转；若影响移动端交互，可保留默认红方视角与顶视模式。
- 高级后期处理、实时环境反射和复杂布料模拟。

## 4. 一期验收标准

### 4.1 规则正确性

- 七类棋子的正常走法和所有上述限制均有自动化测试。
- 固定局面测试至少覆盖：马腿阻挡、象眼阻挡、炮隔一子吃子、炮无架不能吃、将帅照面、被将时解将、走子后自陷将军、绝杀、困毙。
- 每次合法移动只切换一次回合；非法点击不改变棋盘、历史或回合。
- 吃子、悔棋、重开后的棋子数量、位置、当前行棋方和胜负状态一致。
- 动画未结束时重复点击不会造成多步提交、同格双棋或表现与规则状态分离。

### 4.2 视觉与交互

- 首屏可看到完整棋盘，默认视角下无棋子或 UI 遮挡关键交点。
- 不查看汉字时，红黑双方也能通过颜色、材质和轮廓区分；查看底座时能识别全部棋子类型。
- 选中状态、合法落点、最近一步、将军和游戏结束均有明确且互不混淆的反馈。
- 普通移动总时长为 0.3–0.6 秒；吃子主动画为 0.7–1.2 秒。
- 马和炮至少各有一种可辨认的专属移动或吃子效果。
- 特效关闭或降低后仍保留必要的选中、落点和吃子结果反馈。
- `1280×720`、`1440×900`、`390×844` 三个视口下文字不溢出、按钮不重叠、棋盘可操作。
- 鼠标和触控均可完成选子、落子、缩放/旋转与镜头复位；触控目标不小于 40 CSS 像素。

### 4.3 性能与可靠性

- 生产构建成功，无 TypeScript 错误和阻断级 lint 错误。
- 中档桌面设备在完整棋局下目标保持 50 FPS 以上；常见集成显卡或中档手机目标保持 30 FPS 以上。
- 同屏粒子数量、阴影贴图尺寸和设备像素比设置上限；连续吃子不会造成效果对象持续增长。
- 首次可交互目标小于 4 秒（本地或正常宽带、无缓存基准），核心资源失败时显示可理解的错误状态而非空白画布。
- 浏览器控制台无持续报错；重开 20 次、悔棋 20 次后不出现重复监听器、残留动画或明显内存持续增长。

### 4.4 交付物

- 可通过明确命令启动和构建的源码。
- `README.md`：环境要求、安装、开发、测试、构建和操作说明。
- 自动化测试及固定局面数据。
- 至少一组桌面和移动端验收截图。
- 已知限制列表，明确复杂循环判罚、音频和正式美术资产的完成状态。

## 5. 里程碑与排期

按 1 名前端/3D 开发计算，一期预计 6–8 个有效开发日；正式高精资产不计入该排期。

| 里程碑 | 预计时间 | 工作内容 | 完成出口 |
| --- | --- | --- | --- |
| M0 工程与规格冻结 | 0.5 天 | 初始化工程、目录、质量工具；冻结坐标、数据模型和验收局面 | 开发/测试/构建命令可运行，核心类型评审通过 |
| M1 规则闭环 | 1.5–2 天 | 初始局面、合法着法、将军/胜负、回合、历史、悔棋 | 规则测试通过，可用调试 UI 完成一局 |
| M2 3D 棋盘与棋子 | 1.5–2 天 | 棋盘、程序化棋子、汉字、拾取、选中和合法落点 | 桌面与手机可准确选子和落子，双方风格可辨 |
| M3 动画与战斗反馈 | 1–1.5 天 | 移动时间线、通用吃子、马/炮专属效果、镜头和设置 | 动画锁可靠，连续走子无状态错乱，性能达标 |
| M4 完整 UI 与响应式 | 0.5–1 天 | 回合/将军/胜负、悔棋/重开、视角控制、移动布局 | 三个基准视口可完成整局，界面无重叠 |
| M5 测试与发布候选 | 1 天 | E2E、视觉与性能检查、缺陷修复、文档 | 全部一期验收项完成，无阻断级缺陷 |

每个里程碑必须先满足“完成出口”再进入下一阶段。M1 和 M2 可以在核心类型及坐标接口冻结后并行开发，但 M3 不应在规则提交/回滚语义稳定前开始。

## 6. 测试策略

### 6.1 单元测试

- 对每种棋子使用表格驱动测试，覆盖正常、边界、阻挡和越界局面。
- 对 `RuleEngine` 使用不可变性测试：非法走法不改变输入和内部状态。
- 对将军与胜负判断使用人工可核验的最小固定局面，避免只测试完整开局。
- 对 `boardToWorld` 和拾取反向转换做 90 个交点往返测试。
- 对 reducer/状态机测试动画锁、重开、悔棋、游戏结束和过期事件。

### 6.2 集成测试

- 从选子到规则提交再到动画完成，验证一次用户操作只产生一次走子。
- 验证吃子前后场景节点与规则棋子数一致。
- 验证动画中重开、悔棋或关闭特效时，所有异步效果能正确取消。
- 验证设置持久化、相机复位和视口变化不会改变棋局状态。

### 6.3 端到端与视觉测试

- Playwright 覆盖：启动、第一步合法移动、非法移动、吃子、将军提示、悔棋、重开和游戏结束。
- 桌面与手机视口分别截图，人工检查棋盘完整性、文字、遮挡和 WebGL 非空白像素。
- 使用一组确定性走子序列进行回归，保证最终 FEN/局面序列化结果固定。
- 低特效档和设备像素比上限开启时跑一次完整冒烟流程。

### 6.4 手工验收

- 至少由一名熟悉中国象棋规则的人员完成一盘有效对局。
- 检查红黑棋子远、中、近三个距离的识别度。
- 连续快速点击、双击、拖动相机、旋转后落子，确认无误触提交。
- 使用集成显卡设备和一台真实手机验证帧率、发热、触控和页面高度变化。

## 7. 主要风险与应对

| 风险 | 影响 | 早期信号 | 应对措施 |
| --- | --- | --- | --- |
| 规则库边界行为不完整 | 出现非法着法或错误胜负 | 固定局面与预期不一致 | 用适配层隔离；M1 首日完成规则探针；必要时替换实现，不修改 UI |
| 表现动画与棋局状态竞争 | 重复走子、同格双棋、回合错乱 | 快速点击或重开后复现 | 状态先原子提交，动画期间锁输入；所有时间线支持取消和版本校验 |
| 铠甲造型降低棋子辨识度 | 玩家依赖试点或误认 | 中远距离测试频繁看底座 | 固定类型轮廓和高度；始终保留大号汉字；优先精做马、车、炮 |
| 粒子、阴影导致移动端掉帧 | 无法顺畅操作或发热 | 同屏效果时低于 30 FPS | 对象池、粒子上限、单主光阴影、DPR 上限和低画质档 |
| 3D 拾取受视角影响 | 点不中交点或误选 | 边缘棋子错误率高 | 使用扩大且不可见的拾取体；落点使用棋盘平面求交；提供相机复位 |
| 汉字纹理模糊或缺字 | 核心识别失败 | 高 DPR/缩放下发虚 | 打包许可明确的中文字体或预生成纹理图集，禁止依赖系统字体差异 |
| 浏览器/WebGL 加载为空白 | 原型无法启动 | Canvas 无像素或资源异常 | 错误边界、加载状态、资源超时提示；Playwright 做画布像素检查 |
| 美术资产或音频授权不清 | 无法公开发布 | 来源、许可无法追溯 | 一期优先原创程序化资产；每个外部资产记录来源、作者和许可证 |
| 范围膨胀 | 核心规则与可玩性延误 | M2 前开始做 AI/高精模型 | 以验收表为范围基线；可延后项不得阻塞 M5 |

## 8. 二期路线

二期目标是从“可玩原型”提升为“有完整视听品质的单机产品”。

- 替换程序化棋子为正式红黑两套铠甲角色，完成 LOD、贴图压缩和模型规范。
- 为七类棋子制作差异化移动/击杀动画，并补充将军、绝杀和帅旗倒下演出。
- 接入完整音效、背景音乐、音量分组和移动端震动反馈。
- 加入成熟象棋 AI，提供难度等级、限时思考和取消计算机制；AI 在 Web Worker 中运行。
- 完成棋谱列表、回放、局面保存/恢复、FEN 或兼容格式导入导出。
- 增加先后手与棋盘翻转、快捷操作、色觉友好和减少动态效果模式。
- 建立正式资产流水线、性能预算和更多真机兼容矩阵。

二期出口：单机人机对局完整、正式资产替换完毕、中档移动设备稳定 30 FPS，并可作为公开试玩版发布。

## 9. 三期路线

三期目标是加入可运营的在线对战能力，但不让联网逻辑侵入规则和表现层。

- 权威服务器房间、匹配、断线重连、超时判负和观战。
- 服务端复用/镜像 `RuleEngine`，客户端提交走子意图，服务端验证并广播结果。
- 账号、段位、排行榜、对局记录、举报和基础反作弊。
- 好友房、私人房、残局挑战、赛事模式和回放分享。
- 多套棋盘/棋子外观和低带宽资源分包；商业化方案单独立项评审。
- 监控、日志、崩溃上报、数据备份、隐私与未成年人合规。

三期出口：服务器能够权威裁决并恢复对局，弱网和重连场景有自动化验证，客户端无法通过篡改本地状态产生非法着法。

## 10. 开发启动清单

1. 冻结 `Position`、`Piece`、`Move`、`GameState`、`MoveResult` 和坐标方向定义。
2. 建立固定局面数据，先写规则探针和关键失败用例。
3. 初始化 Vite/React/TypeScript、测试和质量命令。
4. 并行实现 `RuleEngine` 与无交互 3D 棋盘/棋子展示。
5. 打通“选中 → 合法落点 → 提交走子 → 动画 → 切换回合”单条纵向流程。
6. 再补充全部规则、吃子、将军/胜负、悔棋和重开。
7. 最后增加表现差异、响应式、性能档位和端到端验收。

一期应始终以“能正确、顺畅地完成一局”为最高优先级。任何不能直接提高规则可靠性、棋局辨识度或核心表现验证价值的工作，默认进入二期候选池。

## 11. 一期执行决策

- 实际工程采用 Vite、React、TypeScript、Three.js、React Three Fiber 与 Drei。
- 规则适配采用 `elephantops@0.1.1`，并锁定精确版本。它负责基础合法着法、将帅照面、自陷将、将军、将死与困毙判断。
- `elephantops` 使用 GPL-3.0-or-later，本期仅按可试玩原型使用。闭源或商业发行前必须完成许可证决策，不能把当前依赖选择直接视为商业发布方案。
- 状态历史由应用层保存完整局面快照，3D 动画仅消费已确认的 `MoveRecord`，不直接修改规则数据。
- 一期美术使用原创程序化几何体和浏览器生成的汉字纹理；二期通过同一组件接口替换为正式 GLB 资产。
- 一期不实现 AI、联网、长将、长捉和争议重复局面裁决，这些能力不会阻塞本地双人核心原型验收。

## 12. 一期执行结果

一期可玩原型已于 2026-08-04 完成，当前交付包含：

- 本地双人完整基础走子闭环、将军/胜负判断、悔棋与重开。
- 程序化 3D 棋盘、红黑两套铠甲造型语言、七类棋子的差异化轮廓与汉字底座。
- 移动弧线、落点反馈、吃子冲击与碎甲粒子，马和炮具备差异化表现。
- 斜视/顶视、镜头复位、音效、特效强度、镜头震动和响应式 HUD。
- `390×844`、`1280×720`、`1440×900` 三档视口的浏览器人工验收。
- 15 项规则、坐标、控制器与记谱单元/集成测试，以及 5 项桌面/移动端 Chromium 端到端测试。

质量命令 `npm run lint`、`npm test`、`npm run build`、`npm run test:e2e` 均通过。生产构建仍有 Three.js 主包大于 500 kB 的非阻断提示，代码拆分、正式 GLB 资产、AI、联网和竞赛级循环裁决按计划留到后续阶段。
